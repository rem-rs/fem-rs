[package]
name    = "fem-assembly"
version = "0.1.0"
edition = "2021"

[dependencies]
fem-core    = { path = "../core" }
fem-mesh    = { path = "../mesh" }
fem-element = { path = "../element" }
fem-space   = { path = "../space" }
fem-linalg  = { path = "../linalg" }
fem-solver  = { path = "../solver" }
fem-amg     = { path = "../amg" }
linlvo      = { git = "https://github.com/rem-rs/linlvo.git" }
nalgebra    = { workspace = true }
libm        = "0.2"
thiserror   = { workspace = true }
fem-linalg-gpu = { path = "../linalg-gpu", optional = true }
wgpu           = { version = "27", optional = true }
reed        = { workspace = true, optional = true }
reed-core   = { workspace = true, optional = true }
reed-cpu    = { workspace = true, optional = true }

[features]
default  = []
parallel = ["dep:rayon", "fem-linalg/parallel"]
gpu      = ["dep:fem-linalg-gpu", "dep:wgpu"]
reed     = ["dep:reed", "dep:reed-core", "dep:reed-cpu"]

[dependencies.rayon]
workspace = true
optional  = true

[dev-dependencies]
fem-linalg = { path = "../linalg" }
fem-regression = { path = "../regression" }
proptest = "1"
